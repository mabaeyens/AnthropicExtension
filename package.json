{
  "name": "anthropic-extension-dev",
  "version": "0.5.5",
  "private": true,
  "description": "Dev-only tooling (ESLint + node:test unit tests) for the AnthropicExtension Qlik Sense extension. NOT shipped — the runtime is plain AMD loaded by Qlik's RequireJS (no build step). package.ps1 zips only the runtime files.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "lint": "eslint js test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
