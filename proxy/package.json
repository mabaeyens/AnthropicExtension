{
  "name": "cm-llm-proxy",
  "version": "2.0.1",
  "description": "HTTPS proxy server to forward Qlik Sense requests to the Anthropic API or a local Ollama model",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "lint": "eslint .",
    "load": "node test/load-test.js --requests 200 --report"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
